import SwiftUI

@main
struct CreativeLogApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        .windowStyle(.titleBar)
        .defaultSize(width: 820, height: 580)
    }
}
